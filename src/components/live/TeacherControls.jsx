/**
 * TeacherControls is now empty/passthrough.
 *
 * All teacher actions (mic, camera, screen, pause/resume, leave) have been
 * moved into the unified ControlBar component at the bottom of the screen.
 *
 * This file is kept to avoid breaking imports. You can delete it later
 * and remove the import from ClassroomUI.jsx.
 */
export default function TeacherControls() {
  return null;
}